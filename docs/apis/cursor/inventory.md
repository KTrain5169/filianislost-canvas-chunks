# `/cursor/inventory`

the `/cursor/inventory` route holds info about the user's cursor inventory.
It only supports one HTTP method: `GET`.

## `GET`

This method require authentication, using the token found in
